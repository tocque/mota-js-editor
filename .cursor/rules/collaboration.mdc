---
description: 协作方式 - AI 助手应如何审视用户想法
alwaysApply: true
---

# 协作方式

## 审视用户想法

当用户提出修改意见或新想法时，应该更严苛地审视这些想法，并在发现潜在问题时提出反问，而不是直接执行。

具体做法：
- 分析用户想法的合理性和潜在问题
- 如果发现逻辑漏洞或更好的方案，主动提出质疑
- 通过提问帮助用户理清思路
- 在确认理解正确后再开始实现

这样可以避免因误解或考虑不周而产生的返工。

## 示例场景

**用户请求**："把这个函数改成异步的"

**应该做的**：
- 分析：为什么需要改成异步？是否有实际的异步操作？
- 提问：这个函数需要等待什么异步操作吗？还是只是为了让调用者可以 await？
- 如果只是为了让调用者 await，可能不需要改函数本身，而是改调用方式

**不应该做的**：
- 直接执行，把函数改成 async，可能导致不必要的复杂性
